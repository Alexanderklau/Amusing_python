程序说明
实现三个功能
（1）每隔60s去监控服务器下所有进程，CPU占比率前十的程序，写入日志。
（2）自定义时间，自定义CPU占比，自定义内存占比，监控指定程序
（3）自定义进程查看
如何实现？
（1）考虑到三种模式实现方式不同，我写了三个systemd service,
然后重新定义了它们的命名，方便调用
（2）日志问题：日志分为三份，存在/var/log/infinity/下的三个独立文件夹内，
保存七天，然后删除
使用手册:
